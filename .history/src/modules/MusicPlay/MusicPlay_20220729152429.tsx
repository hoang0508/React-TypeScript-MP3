import * as React from "react";

export interface MusicPlayProps {}

export default function MusicPlay(props: MusicPlayProps) {
  return (
    <div className="fixed bottom-0 left-0 right-0 z-50 bg-bgColor2">
      Music play
    </div>
  );
}
